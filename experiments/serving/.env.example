# Copy to .env (gitignored) and fill in. Used by docker-compose.yml.
# Telegram bot token from @BotFather:
TOKEN=123456789:AA...your-bot-token...
# URL of the running translation API (uvicorn api:app on a capable host):
SICILIAN_API=http://your-model-host:8000
